spring:
  application:
    name: ddk-example-user
  ai:
    mcp:
      server:
        name: ddk-example-user
        version: 0.2.0
  datasource:
    url: jdbc:h2:mem:users;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always

ddk:
  mybatis:
    db-type: h2
