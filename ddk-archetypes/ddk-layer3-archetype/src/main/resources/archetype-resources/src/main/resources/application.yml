spring:
  application:
    name: ${artifactId}
  # 默认使用内存 H2，开箱即可启动；接入 MySQL 时替换 url / username / password 并引入驱动
  datasource:
    url: jdbc:h2:mem:${artifactId};MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:

ddk:
  mybatis:
    db-type: h2
